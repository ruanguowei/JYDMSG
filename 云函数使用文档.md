# 云函数使用文档

## 目录
1. [swapEvaluationsByName - 交换作品数据](#1-swapevaluationsbyname---交换作品数据)
2. [exportFinalResults - 导出终评排名结果](#2-exportfinalresults---导出终评排名结果)

---

## 1. swapEvaluationsByName - 交换作品数据

### 📋 功能说明

**云函数名：** `swapEvaluationsByName`

**功能：** 根据作品名称交换两个作品的**完整数据**（不仅是评分，而是所有字段）

**使用场景：**
- 两个作品的评分需要互换
- 需要将评分表的作品和结果表的作品进行数据交换
- 批量交换多对作品

---

### 🖥️ 在云开发控制台使用步骤

#### 步骤1：打开云开发控制台
1. 登录微信公众平台或直接访问：https://console.cloud.tencent.com/tcb
2. 选择对应的云环境（生产环境或测试环境）

#### 步骤2：进入云函数测试界面
1. 左侧菜单 → 点击「云函数」
2. 在云函数列表中找到 `swapEvaluationsByName`
3. 点击右侧的「测试」按钮

#### 步骤3：准备作品名称
1. 打开「数据库」
2. 进入对应的表（如 pottery_submissions_for_final）
3. 找到要交换的作品，**复制作品名称**（artworkName 字段）
   - ⚠️ 必须完整复制，包括书名号《》等标点符号
   - 建议直接复制粘贴，不要手动输入

#### 步骤4：编写 JSON 参数
在测试窗口中粘贴 JSON 参数（见下方案例）

#### 步骤5：执行测试
1. 点击「测试」按钮
2. 等待执行完成（通常5-15秒）
3. 查看返回结果

#### 步骤6：验证结果
1. 返回数据库，查看作品数据是否已交换
2. 检查 evaluations 等字段是否正确

---

### 📝 调用参数格式

#### 基本结构：
```json
{
  "targetTable": "默认表名（可选）",
  "mode": "执行模式（可选）",
  "swaps": [
    {
      "workName1": "作品1名称",
      "table1": "作品1所在表（可选）",
      "workName2": "作品2名称",
      "table2": "作品2所在表（可选）"
    }
  ]
}
```

#### 参数说明：

| 参数 | 类型 | 必需 | 默认值 | 说明 |
|------|------|------|--------|------|
| `targetTable` | string | ⚪ | `pottery_submissions_clean` | 默认表（当swap未指定table时使用） |
| `mode` | string | ⚪ | `best-effort` | 执行模式：`best-effort`（允许部分失败） / `all-or-nothing`（全部成功或全部取消） |
| `swaps` | array | ✅ | - | 交换对数组，至少1对 |
| `swaps[].workName1` | string | ✅ | - | 作品1的名称（完整，含标点） |
| `swaps[].table1` | string | ⚪ | targetTable | 作品1所在的表 |
| `swaps[].workName2` | string | ✅ | - | 作品2的名称（完整，含标点） |
| `swaps[].table2` | string | ⚪ | targetTable | 作品2所在的表 |

---

### 💡 使用案例

#### 案例1：同表交换（最简单）

**需求：** 在清洗表中，交换"入海"和"绽放"两个作品的数据

**JSON：**
```json
{
  "targetTable": "pottery_submissions_clean",
  "swaps": [
    {
      "workName1": "入海",
      "workName2": "绽放"
    }
  ]
}
```

**执行步骤：**
1. 云函数 → `swapEvaluationsByName` → 测试
2. 粘贴上述 JSON
3. 点击测试
4. 查看返回结果

**返回结果示例：**
```json
{
  "success": true,
  "message": "批量交换完成：1对全部成功",
  "data": {
    "succeeded": 1,
    "failed": 0,
    "results": [
      {
        "work1": {
          "id": "abc123",
          "originalName": "入海",
          "newName": "绽放"
        },
        "work2": {
          "id": "def456",
          "originalName": "绽放",
          "newName": "入海"
        }
      }
    ]
  }
}
```

---

#### 案例2：跨表交换（评分表 ⟷ 结果表）

**需求：** 将评分表中的"入海"与结果表中的"四季藻井·数字纹章"交换

**JSON：**
```json
{
  "swaps": [
    {
      "workName1": "入海",
      "table1": "pottery_submissions_for_final",
      "workName2": "四季藻井·数字纹章",
      "table2": "pottery_submissions_final"
    }
  ]
}
```

**执行步骤：**
1. 云函数 → `swapEvaluationsByName` → 测试
2. 粘贴上述 JSON
3. 点击测试

**执行后的效果：**
- `pottery_submissions_for_final` 表 ID=abc123 的记录 → 现在是"四季藻井·数字纹章"的数据
- `pottery_submissions_final` 表 ID=def456 的记录 → 现在是"入海"的数据

**控制台日志：**
```
=== 开始批量交换作品评分（按名称） ===
默认表: pottery_submissions_clean
执行模式: best-effort
交换对数: 1

--- 查找第0对 ---
作品1名称: "入海" (表: pottery_submissions_for_final)
作品2名称: "四季藻井·数字纹章" (表: pottery_submissions_final)
  ✅ 作品1找到: abc123...
  ✅ 作品2找到: def456...
  ✅ 第0对查找成功

--- 执行第0对 ---
  准备交换完整数据:
    表1: pottery_submissions_for_final - "入海" (ID: abc123...)
    表2: pottery_submissions_final - "四季藻井·数字纹章" (ID: def456...)
  ✅ 更新作品1成功：用作品2的数据覆盖
  ✅ 更新作品2成功：用作品1的数据覆盖
  🎉 第0对交换完成
    表1 ID abc123... 现在是: "四季藻井·数字纹章"的数据
    表2 ID def456... 现在是: "入海"的数据
```

---

#### 案例3：批量交换多对作品

**需求：** 同时交换3对作品

**JSON：**
```json
{
  "mode": "best-effort",
  "swaps": [
    {
      "workName1": "入海",
      "table1": "pottery_submissions_for_final",
      "workName2": "四季藻井·数字纹章",
      "table2": "pottery_submissions_final"
    },
    {
      "workName1": "绽放",
      "table1": "pottery_submissions_for_final",
      "workName2": "我们睡在词的背面",
      "table2": "pottery_submissions_final"
    },
    {
      "workName1": "雪山瓷韵",
      "table1": "pottery_submissions_for_final",
      "workName2": "筇竹茶具",
      "table2": "pottery_submissions_final"
    }
  ]
}
```

**执行后：**
- 3对作品的数据全部互换
- 如果某对查找失败，其他成功的仍会执行（best-effort 模式）

---

### ⚠️ 重要注意事项

1. **作品名称必须完全准确**
   - ✅ 包括书名号：`《回望工业时代系列一》`
   - ❌ 不能缺少：`回望工业时代系列一`
   - 建议从数据库直接复制粘贴

2. **交换不可自动撤销**
   - 交换后数据立即生效
   - 如需还原，再次交换即可

3. **只有ID不交换**
   - 数据库记录的 `_id` 保持不变
   - 其他所有字段都会交换

4. **支持的表名**
   - `pottery_submissions` - 原始提交表
   - `pottery_submissions_clean` - 清洗表
   - `pottery_submissions_preliminary` - 初评结果表
   - `pottery_submissions_for_final` - 终评评分表
   - `pottery_submissions_final` - 终评结果表

5. **建议先单对测试**
   - 第一次使用时，先测试1对交换
   - 验证成功后再批量操作

---

## 2. exportFinalResults - 导出终评排名结果

### 📋 功能说明

**云函数名：** `quickstartFunctions`（统一云函数）

**功能：** 从指定表导出终评结果，自动计算平均分、排序、生成排名

**使用场景：**
- 导出所有终评作品的评分情况（545件）
- 导出入围作品的排名结果（317件）
- 生成获奖名单

---

### 🖥️ 在云开发控制台使用步骤

#### 步骤1：打开云开发控制台
1. 登录微信公众平台或访问：https://console.cloud.tencent.com/tcb
2. 选择**生产环境**：`jdzyzdmsg-5g4rgrjl2008796f`

#### 步骤2：进入云函数测试界面
1. 左侧菜单 → 点击「云函数」
2. 找到 `quickstartFunctions` 云函数
3. 点击右侧的「测试」按钮

#### 步骤3：输入 JSON 参数
在测试窗口中粘贴 JSON（见下方案例）

#### 步骤4：执行测试
1. 点击「测试」按钮
2. 等待执行完成（约10-30秒，取决于数据量）

#### 步骤5：获取下载链接
1. 在返回结果中找到 `downloadUrl` 字段
2. 复制链接
3. 在浏览器中打开链接
4. 文件自动下载

---

### 📝 调用参数格式

#### 基本结构：
```json
{
  "type": "exportFinalResults",
  "sourceTable": "数据来源表（可选）"
}
```

#### 参数说明：

| 参数 | 类型 | 必需 | 默认值 | 说明 |
|------|------|------|--------|------|
| `type` | string | ✅ | - | 固定值：`exportFinalResults` |
| `sourceTable` | string | ⚪ | `pottery_submissions_for_final` | 数据来源表 |

#### sourceTable 可选值：

| 表名 | 说明 | 数据量 | 用途 |
|------|------|--------|------|
| `pottery_submissions_for_final` | 终评评分表 | 约545件 | 所有参与终评的作品 |
| `pottery_submissions_final` | 终评结果表 | 约317件 | 筛选后的入围作品 |

---

### 💡 使用案例

#### 案例1：导出所有终评作品（545件）

**需求：** 查看所有参与终评评分的作品及其评分情况

**JSON：**
```json
{
  "type": "exportFinalResults"
}
```

或明确指定：
```json
{
  "type": "exportFinalResults",
  "sourceTable": "pottery_submissions_for_final"
}
```

**执行步骤：**
1. 云函数 → `quickstartFunctions` → 测试
2. 粘贴上述 JSON
3. 点击测试
4. 等待10-30秒

**返回结果示例：**
```json
{
  "success": true,
  "downloadUrl": "https://6a64-jdzyzdmsg-5g4rgrjl2008796f-1378111268.tcb.qcloud.la/admin_exports/final/终评结果_评分表_20251101_2356.csv?sign=xxx",
  "fileName": "终评结果_评分表_20251101_2356.csv",
  "sourceTable": "pottery_submissions_for_final",
  "recordCount": 545,
  "videoCount": 16,
  "message": "成功导出 545 条终评结果（来自评分表，含16件视频作品）"
}
```

**下载文件：**
1. 复制 `downloadUrl` 的值
2. 在浏览器中打开这个链接
3. CSV 文件自动下载
4. ⚠️ 链接有效期：**2小时**

**文件内容：**
- 545件作品的详细评分
- 按总分从高到低排序
- 排名：1, 2, 3...545

---

#### 案例2：导出入围作品排名（317件）⭐ 推荐

**需求：** 导出最终入围的317件作品，查看获奖排名

**JSON：**
```json
{
  "type": "exportFinalResults",
  "sourceTable": "pottery_submissions_final"
}
```

**执行步骤：**
1. 云函数 → `quickstartFunctions` → 测试
2. 粘贴上述 JSON
3. 点击测试

**返回结果示例：**
```json
{
  "success": true,
  "downloadUrl": "https://...终评结果_结果表_20251101_2358.csv?sign=xxx",
  "fileName": "终评结果_结果表_20251101_2358.csv",
  "sourceTable": "pottery_submissions_final",
  "recordCount": 317,
  "videoCount": 12,
  "message": "成功导出 317 条终评结果（来自结果表，含12件视频作品）"
}
```

**文件内容：**
- 317件入围作品
- 按总分从高到低排序
- 排名：1, 2, 3...317
- 包含获奖等级（一二三等奖、优秀奖、入围奖）

**CSV 表格列：**
```
排名 | 作品类型 | 作品名称 | 作者姓名 | 学校 | 学校省份 | 
作品类别 | 评委数量 | 主题契合度平均分 | 创意表现力平均分 | 
工艺材料平均分 | 美感实用性平均分 | 总分平均分 | 获奖等级
```

---

### 📊 导出数据说明

#### 评分计算规则：
```
去最高最低分后的平均分：
├─ 如果评委数 < 3：直接计算平均分
└─ 如果评委数 ≥ 3：去掉最高分和最低分后计算平均分
```

#### 排序规则（从高到低）：
```
第一优先级：总分平均分（降序）
第二优先级：主题契合度平均分（降序）
第三优先级：创意表现力平均分（降序）
第四优先级：工艺材料平均分（降序）
```

#### 获奖等级划分：
```
排名 1-10：一等奖
排名 11-30：二等奖
排名 31-60：三等奖
排名 61-100：优秀奖
排名 101+：入围奖
```

---

### 📂 导出文件位置

**云存储路径：**
```
admin_exports/final/终评结果_评分表_20251101_2356.csv
admin_exports/final/终评结果_结果表_20251101_2358.csv
```

**查看方法：**
1. 云开发控制台 → 云存储 → 文件管理
2. 进入 `admin_exports/final/` 文件夹
3. 可以看到所有导出的文件

---

### ⚠️ 重要注意事项

1. **表名不能错**
   - `pottery_submissions_for_final` - 评分表（545件，所有作品）
   - `pottery_submissions_final` - 结果表（317件，入围作品）
   - ⚠️ 两个表名很相似，注意区分！

2. **下载链接有效期**
   - 临时链接有效期：2小时
   - 过期后需要重新调用云函数生成

3. **文件永久保存**
   - 虽然链接过期，但文件永久保存在云存储中
   - 可以在云存储控制台重新获取链接

4. **每次调用都生成新文件**
   - 不会覆盖旧文件
   - 文件名包含时间戳，自动区分

---

## 🎯 常见问题

### Q1：如何知道应该从哪个表导出？

**A：根据需求选择**
- 想看所有终评作品 → `pottery_submissions_for_final`（545件）
- 想看入围作品排名 → `pottery_submissions_final`（317件）

---

### Q2：swapEvaluationsByName 找不到云函数？

**A：** 需要先部署
1. 右键 `cloudfunctions/swapEvaluationsByName` 文件夹
2. 上传并部署：云端安装依赖
3. 等待部署完成

---

### Q3：作品名称不匹配怎么办？

**A：** 从数据库复制准确名称
1. 数据库 → 对应的表
2. 找到作品记录
3. 复制 `artworkName` 字段的值（完整复制）
4. 粘贴到 JSON 中

---

### Q4：交换可以撤销吗？

**A：** 可以，再次交换即可还原
- 交换A和B后
- 再次执行交换A和B
- 数据恢复原状

---

### Q5：下载链接过期了怎么办？

**A：** 两种方法
1. 重新调用云函数（会生成新文件）
2. 在云存储控制台找到文件，重新获取链接

---

## 📞 技术支持

如遇问题，请记录：
- 使用的 JSON 参数
- 返回的错误信息
- 控制台日志信息

---

## 📝 快速参考

### swapEvaluationsByName 快速模板：

**同表交换：**
```json
{
  "targetTable": "pottery_submissions_clean",
  "swaps": [
    { "workName1": "作品A", "workName2": "作品B" }
  ]
}
```

**跨表交换：**
```json
{
  "swaps": [
    {
      "workName1": "作品A",
      "table1": "pottery_submissions_for_final",
      "workName2": "作品B",
      "table2": "pottery_submissions_final"
    }
  ]
}
```

---

### exportFinalResults 快速模板：

**导出所有作品：**
```json
{
  "type": "exportFinalResults"
}
```

**导出入围作品：**
```json
{
  "type": "exportFinalResults",
  "sourceTable": "pottery_submissions_final"
}
```

---

**文档结束**


